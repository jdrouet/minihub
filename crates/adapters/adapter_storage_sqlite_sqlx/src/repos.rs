//! Repository implementations for `SQLite`.
//!
//! TODO(M2): Implement `SqliteEntityRepository` — implements `EntityRepository` port.
//! TODO(M2): Implement `SqliteDeviceRepository` — implements `DeviceRepository` port.
//! TODO(M2): Implement `SqliteAreaRepository` — implements `AreaRepository` port.
//! TODO(M2): Implement `SqliteEventStore` — implements `EventStore` port.
