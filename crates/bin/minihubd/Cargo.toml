[package]
name = "minihubd"
description = "Composition root — wires all adapters together and starts the minihub server."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
minihub-domain = { workspace = true }
minihub-app = { workspace = true }
minihub-adapter-http-axum = { workspace = true }
minihub-adapter-storage-sqlite-sqlx = { workspace = true }
# minihub-adapter-mqtt = { workspace = true }  # Enable when MQTT support is added
axum = { workspace = true }
tokio = { version = "1", features = ["macros", "rt-multi-thread", "net"] }

[dev-dependencies]
http-body-util = "0.1"
serde_json = { workspace = true }
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
tower = { workspace = true }

[lints]
workspace = true
